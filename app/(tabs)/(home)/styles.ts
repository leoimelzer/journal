import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginHorizontal: 18,
    marginVertical: 18,
    gap: 10
  },
  navigation: {},
  route: {
    fontSize: 18,
    fontWeight: 'bold'
  },
  redirects: {
    gap: 3
  }
});
